name: Approval Made

on:
  pull_request:
    branches:
      - master
  pull_request_review:
    types:
      - submitted

jobs:
  approved:
    if: ${{ github.event.review.state == 'approved' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This PR was approved"
